# Architecture

The system consists of two main components:
1. **Environment**: Represents the physical world where the agent operates. It keeps track of the positions that are currently "dirty".
2. **HomeCleaningRobot (Agent)**: Represents the intelligent agent. It perceives the environment, makes a decision based on its perception, and acts on the environment or its own state.
